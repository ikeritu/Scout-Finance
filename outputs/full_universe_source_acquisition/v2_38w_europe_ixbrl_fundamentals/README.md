# v2.38W Europe iXBRL fundamentals

Real accounts-document fetch and targeted iXBRL/ESEF extraction for the Companies House companies confirmed in v2.38V. Only a fixed, closed list of standard ifrs-full:* concepts is extracted, only from the undimensioned "total" context of the most recent reporting period -- never OCR, never a PDF, never a guess. No scoring, ranking, or recommendations. Real extracted values and the raw fetched document stay out of git (see .gitignore); only the aggregate coverage report is committed.
